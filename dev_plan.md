# 迁移到 AI Builder Space 平台开发计划

## 一、当前实现分析

### 1.1 当前架构
- **API 调用方式**: 直接使用 `google-genai` Python SDK 调用 Gemini API
- **使用的模型**:
  - `gemini-3-flash-preview`: 用于设计规划（带 Thinking Mode，HIGH 级别）
  - `gemini-2.5-flash-image`: 用于图像生成（支持 16:9 比例）
- **认证方式**: 使用 `GEMINI_API_KEY` 环境变量
- **调用位置**:
  - 规划阶段：`main.py` 第 130-139 行
  - 图像生成：`main.py` 第 154-164 行

### 1.2 关键功能特性
- Thinking Mode（HIGH 级别）用于复杂规划任务
- Google Search 工具集成
- JSON 格式输出（`response_mime_type="application/json"`）
- 图像生成（`response_modalities=["IMAGE"]`）
- 指数退避重试机制（处理 503 错误）

## 二、AI Builder Space 平台调研

### 2.1 平台 API 架构
- **API 格式**: OpenAI 兼容的 Chat Completions API (`/v1/chat/completions`)
- **认证方式**: Bearer Token（`AI_BUILDER_TOKEN`）
- **API 基础端点**: `https://space.ai-builders.com/backend` ✅ **已确认**
- **OpenAPI 文档**: `https://space.ai-builders.com/backend/openapi.json`
- **主要端点**:
  - `/v1/chat/completions`: 文本生成和对话
  - `/v1/images/generations`: 图像生成
  - `/v1/images/edits`: 图像编辑
  - `/v1/search/`: 网络搜索（Tavily）
  - `/v1/models`: 获取支持的模型列表

### 2.2 支持的模型列表 ✅ **已通过 API 验证**
根据 API 调用结果（`GET /v1/models`），AI Builder Space 支持以下模型：

**文本生成模型**:
1. **`deepseek`**: 快速且经济的聊天完成（Always available）
2. **`supermind-agent-v1`**: 多工具代理，带网络搜索和 Gemini handoff
3. **`gemini-2.5-pro`**: 直接访问 Google 的 Gemini 模型（OpenAI 兼容）
4. **`gemini-3-flash-preview`**: ✅ **快速 Gemini 推理模型（已确认支持！）**
5. **`gpt-5`**: 透传到 OpenAI 兼容的提供商
6. **`grok-4-fast`**: 透传到 X.AI 的 Grok API

**图像生成模型**:
1. **`gpt-image-1.5`**: OpenAI 图像生成模型
2. **`gemini-2.5-flash-image`**: ✅ **Gemini 图像生成模型（已确认支持！）**

### 2.3 模型兼容性分析

#### ✅ 规划模型迁移可行性 - **完全兼容！**
- **当前**: `gemini-3-flash-preview`（带 Thinking Mode）
- **平台支持**: ✅ **`gemini-3-flash-preview` 已在支持的模型列表中**
- **兼容性**: 
  - 可以直接使用相同的模型名称
  - Gemini 特定设置可通过 `extra_body.gemini` 传递（需要验证 Thinking Mode 配置方式）
  - 支持工具调用和网络搜索（通过 `supermind-agent-v1` 或直接配置）

#### ✅ 图像生成模型迁移可行性 - **完全兼容！**
- **当前**: `gemini-2.5-flash-image`（专用图像生成模型）
- **平台支持**: ✅ **`gemini-2.5-flash-image` 已在支持的模型列表中**
- **API 端点**: ✅ **`/v1/images/generations` 端点已存在**
- **兼容性**:
  - 可以直接使用相同的模型名称 `gemini-2.5-flash-image`
  - 支持通过 `size` 参数设置图像比例（需验证 16:9 格式，如 `1536x1024`）
  - 支持 `b64_json` 或 `url` 响应格式
  - ⚠️ **待验证**: 具体参数格式和响应结构

### 2.4 API 调用方式差异

#### 当前方式（Google GenAI SDK）
```python
from google import genai
from google.genai import types

client = genai.Client(api_key=os.getenv("GEMINI_API_KEY"))
response = client.models.generate_content(
    model="gemini-3-flash-preview",
    contents=prompt,
    config=types.GenerateContentConfig(
        response_mime_type="application/json",
        thinking_config=types.ThinkingConfig(thinking_level="HIGH"),
        tools=[types.Tool(googleSearch=types.GoogleSearch())]
    )
)
```

#### AI Builder Space 方式（OpenAI 兼容 API）

**文本生成（规划）**:
```python
import requests

BASE_URL = "https://space.ai-builders.com/backend"
headers = {
    "Authorization": f"Bearer {os.getenv('AI_BUILDER_TOKEN')}",
    "Content-Type": "application/json"
}

# 规划调用
response = requests.post(
    f"{BASE_URL}/v1/chat/completions",
    headers=headers,
    json={
        "model": "gemini-3-flash-preview",  # ✅ 直接支持
        "messages": [{"role": "user", "content": prompt}],
        "temperature": 0.7,
        "max_tokens": 4096,
        # Gemini 特定设置（如果支持）
        "extra_body": {
            "gemini": {
                "response_mime_type": "application/json",
                "thinking_config": {"thinking_level": "HIGH"}
            }
        }
    }
)
```

**图像生成**:
```python
# 图像生成调用
response = requests.post(
    f"{BASE_URL}/v1/images/generations",
    headers=headers,
    json={
        "prompt": full_prompt,
        "model": "gemini-2.5-flash-image",  # ⚠️ 待验证是否支持
        "size": "1536x1024",  # 16:9 比例
        "n": 1,
        "response_format": "b64_json"  # 或 "url"
    }
)
```

## 三、迁移计划

### 3.1 阶段一：可行性验证（优先级：高）

#### 任务 1.1: 验证图像生成 API 调用 ✅ 模型已确认支持
- **目标**: 验证图像生成 API 的完整功能
- **已完成**: 
  1. ✅ 已确认 `/v1/images/generations` 端点存在
  2. ✅ 已确认 `gemini-2.5-flash-image` 模型在支持列表中
- **待测试**:
  1. 实际调用 API，验证 `gemini-2.5-flash-image` 模型可用性
  2. 验证 16:9 比例支持（`size: "1536x1024"`）
  3. 测试响应格式（`b64_json` vs `url`）
  4. 验证响应数据结构是否与当前实现兼容

#### 任务 1.2: 测试规划模型迁移 ✅ 模型已支持
- **目标**: 验证 `gemini-3-flash-preview` 在平台上的功能完整性
- **方法**:
  1. ✅ 已确认 `gemini-3-flash-preview` 在支持列表中
  2. 测试 JSON 输出格式（通过 `extra_body.gemini.response_mime_type`）
  3. 验证 Thinking Mode（通过 `extra_body.gemini.thinking_config`）
  4. 测试工具调用（Google Search）是否可用
  5. 验证 `extra_body.gemini` 参数格式是否正确
- **预期结果**: 确认所有功能（JSON 输出、Thinking Mode、工具调用）正常工作

### 3.2 阶段二：代码重构（优先级：中）

#### 任务 2.1: 创建统一的 API 客户端抽象层
- **目标**: 封装 API 调用，便于切换不同后端
- **实现**:
  ```python
  # 新建文件: services/ai_client.py
  class AIClient:
      def __init__(self, provider="ai_builder_space"):
          self.provider = provider
          self.token = os.getenv("AI_BUILDER_TOKEN")
      
      async def generate_plan(self, prompt, state):
          """生成设计规划"""
          # 根据 provider 选择不同的实现
          pass
      
      async def generate_image(self, prompt):
          """生成图像"""
          # 根据 provider 选择不同的实现
          pass
  ```

#### 任务 2.2: 实现 AI Builder Space 客户端
- **目标**: 实现 OpenAI 兼容的 API 调用
- **关键点**:
  - 使用 `requests` 或 `httpx` 进行 HTTP 调用
  - 处理 Bearer Token 认证
  - 适配 OpenAI 格式的消息结构
  - 处理流式响应（如需要）

#### 任务 2.3: 适配图像生成 ✅ API 已存在
- **目标**: 实现图像生成 API 调用
- **实现步骤**:
  1. 实现 `/v1/images/generations` 端点调用
  2. 处理响应格式（`b64_json` 或 `url`）
  3. 适配 16:9 比例参数（`size: "1536x1024"`）
  4. 验证模型参数（测试是否支持 `gemini-2.5-flash-image`）
- **备选方案**: 如果 Gemini 图像模型不支持，使用平台默认图像模型或保留混合方案

### 3.3 阶段三：功能优化（优先级：低）

#### 任务 3.1: 错误处理和重试机制
- **目标**: 适配 AI Builder Space 的错误响应格式
- **内容**: 
  - 更新重试逻辑以处理新的错误码
  - 添加平台特定的错误处理

#### 任务 3.2: 性能优化
- **目标**: 利用平台特性优化性能
- **内容**:
  - 测试并发请求限制
  - 优化请求批处理
  - 利用流式响应提升用户体验

#### 任务 3.3: 配置管理
- **目标**: 支持多环境配置
- **内容**:
  - 添加环境变量切换（`AI_PROVIDER=ai_builder_space` 或 `google_direct`）
  - 更新 `.env` 示例文件
  - 更新部署文档

## 四、风险评估与应对

### 4.1 风险项更新

#### 风险 1: 图像生成 API 参数格式差异 ✅ 已大幅降低风险
- **概率**: 低（模型已确认支持）
- **影响**: 低（主要是参数格式适配问题）
- **应对**:
  1. ✅ `gemini-2.5-flash-image` 模型已确认在支持列表中
  2. ✅ API 端点已确认存在
  3. 测试 API 调用，验证参数格式（`size`, `response_format` 等）
  4. 适配响应数据结构（如需要）

#### 风险 2: Thinking Mode 配置方式不确定 ✅ 已降低风险
- **概率**: 低（模型已支持）
- **影响**: 低（可通过 `extra_body.gemini` 传递配置）
- **应对**:
  1. ✅ `gemini-3-flash-preview` 已确认支持
  2. 测试 `extra_body.gemini.thinking_config` 配置方式
  3. 如不支持，模型默认推理能力应该足够
  4. 优化 prompt 设计作为补充

#### 风险 3: API 格式差异导致功能缺失
- **概率**: 低
- **影响**: 中
- **应对**:
  1. 详细对比 API 功能
  2. 实现适配层处理差异
  3. 保留回退机制

### 4.2 技术债务
- 需要维护两套 API 客户端代码（过渡期）
- 测试覆盖需要扩展以支持两种后端

## 五、实施时间表

### 第一周：调研与验证
- Day 1-2: 完成可行性验证（图像生成、规划模型测试）
- Day 3-4: 编写测试脚本，验证核心功能
- Day 5: 评估结果，决定迁移策略

### 第二周：核心迁移
- Day 1-2: 实现 API 客户端抽象层
- Day 3-4: 实现 AI Builder Space 客户端（规划功能）
- Day 5: 实现图像生成适配（或混合方案）

### 第三周：测试与优化
- Day 1-3: 端到端测试，修复问题
- Day 4-5: 性能优化，文档更新

## 六、成功标准

1. ✅ 所有现有功能正常工作
2. ✅ 性能不低于当前实现
3. ✅ 代码结构清晰，易于维护
4. ✅ 支持环境变量切换后端
5. ✅ 文档完整更新

## 七、下一步行动

1. **✅ 已完成**: 确认 `gemini-3-flash-preview` 模型支持
2. **✅ 已完成**: 确认图像生成 API (`/v1/images/generations`) 存在
3. **立即行动**: 
   - 获取 `AI_BUILDER_TOKEN`
   - 编写测试脚本验证图像生成模型支持（`gemini-2.5-flash-image`）
   - 测试 `extra_body.gemini` 参数格式和 Thinking Mode 配置
   - 验证 JSON 输出格式和工具调用功能
4. **开始迁移**: 基于验证结果，开始实现 API 客户端

## 八、关键发现总结

### ✅ 已完全确认的支持（通过 API 验证）
1. **`gemini-3-flash-preview` 模型**: ✅ 完全支持，可直接使用
2. **`gemini-2.5-flash-image` 模型**: ✅ 完全支持，可直接使用
3. **图像生成 API**: ✅ `/v1/images/generations` 端点存在且模型已确认
4. **API Base URL**: ✅ `https://space.ai-builders.com/backend`
5. **Gemini 特定配置**: 可通过 `extra_body.gemini` 传递

### ⚠️ 待验证项（技术细节）
1. `extra_body.gemini` 参数的具体格式和可用选项
2. Thinking Mode 配置的正确方式（`thinking_config`）
3. JSON 输出格式的配置方式（`response_mime_type`）
4. 图像生成 API 的具体参数格式（`size`, `response_format` 等）
5. 响应数据结构是否与当前实现完全兼容

### 📊 模型支持验证结果
通过调用 `GET /v1/models` API，确认平台支持 **8 个模型**：
- 6 个文本生成模型（包括 `gemini-3-flash-preview`）
- 2 个图像生成模型（包括 `gemini-2.5-flash-image`）

**结论**: 迁移的技术风险已大幅降低，两个核心模型都已确认支持！

---

**文档版本**: v2.1  
**创建日期**: 2025-12-31  
**最后更新**: 2025-12-31  
**更新说明**: 
- v2.1: 通过实际 API 调用验证，确认 `gemini-2.5-flash-image` 和 `gemini-3-flash-preview` 均完全支持
- v2.0: 根据最新 OpenAPI 文档更新，确认 `gemini-3-flash-preview` 和图像生成 API 支持

